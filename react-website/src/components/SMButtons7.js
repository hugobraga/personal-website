import React from "react";
import { Button } from "@material-ui/core";

const SMButtons7 = () => {
  return (
    <Button variant="contained" color="primary">
      Primary
    </Button>
  );
};

export default SMButtons7;
